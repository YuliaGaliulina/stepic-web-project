
{% block content %}
    {% for post in posts %}
        <div class="question">
                <div class="date">
                    {{ question.added_at }}
                </div>
                <h2><a href="{% url 'new' %}">{{ question.title }}</a></h2>
        </div>
    {% endfor %}
{% endblock %}
